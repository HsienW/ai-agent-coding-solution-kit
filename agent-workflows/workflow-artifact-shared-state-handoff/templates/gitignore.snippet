# Local multi-agent workflow runtime artifacts
# Keep runtime state, agent results, evidence, and traces out of Git by default.
.agent-runtime/

# Optional local runtime exports
.agent-runtime-exports/
